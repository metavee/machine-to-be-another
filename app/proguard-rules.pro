# Add project specific ProGuard rules here.
# Minification is disabled for the release build, so this file is currently unused
# but kept so the release build type has a valid proguardFiles reference.
